version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: ytnafrica-postgres
    environment:
      POSTGRES_DB: ytnafrica
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Kalimbwejoel
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ytnafrica-network

  # Spring Boot Backend
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ytnafrica-backend
    ports:
      - "8282:8282"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/ytnafrica
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: Kalimbwejoel
      JAVA_OPTS: "-Xms256m -Xmx1024m"
    volumes:
      - ./uploads:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - ytnafrica-network
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  ytnafrica-network:
    driver: bridge

